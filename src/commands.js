const ADD = 'ADD';
const REMOVE = 'REMOVE';
const HALT = 'HALT';
const MOVE = 'MOVE';
const RESUME = 'RESUME';
const SCHEDULE = 'SCHEDULE';
const SKIP = 'SKIP';
const STATUS = 'STATUS';
const TERMINATE = 'TERMINATE';
const STOP = 'STOP'
const START = 'START'
const SET = 'SET'

module.exports = {
  ADD,
  REMOVE,
  HALT,
  MOVE,
  RESUME,
  SCHEDULE,
  SKIP,
  STATUS,
  TERMINATE,
  START,
  STOP,
  SET
};
