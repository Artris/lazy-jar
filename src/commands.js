const ADD = 'ADD';
const REMOVE = 'REMOVE';
const HALT = 'HALT';
const MOVE = 'MOVE';
const RESUME = 'RESUME';
const SCHEDULE = 'SCHEDULE';
const SKIP = 'SKIP';
const STATUS = 'STATUS';
const TERMINATE = 'TERMINATE';

module.exports = {
  ADD,
  REMOVE,
  HALT,
  MOVE,
  RESUME,
  SCHEDULE,
  SKIP,
  STATUS,
  TERMINATE
};
